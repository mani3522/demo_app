<%= form_for(@staff_subject) do |f| %>
  <% if @staff_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@staff_subject.errors.count, "error") %> prohibited this staff_subject from being saved:</h2>

      <ul>
      <% @staff_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :staff_id %><br>
    <%= f.number_field :staff_id %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.number_field :subject_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
